/**
 * Type definitions for media device configuration.
 * Includes:
 * - Device ID and label
 * - WebRTC device types
 *
 * By Kunal Das
 */

export interface MediaDevice {
  deviceId: string;
  label: string;
}
